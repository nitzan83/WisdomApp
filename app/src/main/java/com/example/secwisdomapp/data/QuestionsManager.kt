package com.example.secwisdomapp.data

object QuestionsManager {
    // make this class a singleton that in first init loads data from file

    // at every data add flush data to file
    private lateinit var questions: MutableList<String>

    init {
        questions =
    }
}